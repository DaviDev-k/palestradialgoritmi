#include <bits/stdc++.h>

// constraints
#define MAXN 100000
#define MAXP 1000

// input data
int N, i;
char P[MAXP + 1];
int M;

struct Node {
	bool val
};

int main() {
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	
	assert(1 == scanf("%d", &N));
	for (i = 0; i < N; i++) {
		assert(2 == scanf("%d %s", &M, P));
		
		
	}
	
	printf("%d\n", 42); // print the result
	return 0;
}
